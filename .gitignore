# The built binary, at the root only: an unanchored "brief" would also
# swallow any directory that came to be named brief.
/brief

# Where find -ask keeps the ask session behind each choice.
.brief/
